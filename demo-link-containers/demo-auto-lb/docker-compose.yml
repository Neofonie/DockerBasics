version: '2'
services:
    web:
        image: neofonie/demo-server

    loadbalancer:
        image: neofonie/demo-auto-lb
        depends_on:
            - web
        environment:
            - COMPOSE_PROJECT=demoautolb
        links:
            - web
        ports:
            - 42080:8080
        volumes:
            - /var/run/docker.sock:/var/run/docker.sock

    events:
        image: neofonie/demo-lb-events
        depends_on:
            - loadbalancer
        environment:
            - COMPOSE_PROJECT=demoautolb
        volumes:
            - /var/run/docker.sock:/var/run/docker.sock
